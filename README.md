# selenium-junit5-template

Desktop testing:<br>
Using Selenium + JUnit5

How to run on Docker Template:

run command "docker build ."
run command "docker run -e PROFILE=localhost <container id>"

"by Alexey Shnyakin"
